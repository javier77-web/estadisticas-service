apiVersion: v1
kind: Service
metadata:
  name: casino-estadisticas
  namespace: vidalcasino
spec:
  type: ClusterIP
  selector:
    app: casino-estadisticas
  ports:
    - port: 8006
      targetPort: 8006
      protocol: TCP